/* ============================================================
   国情与世界 · 自动补充条目
   ------------------------------------------------------------
   本文件由「每周自动补充最新战略布局/趋势」自动化任务追加，
   主数据文件 js/data-country.js 保持不变，降低自动写入风险。
   ------------------------------------------------------------
   每条条目对象结构（必须与下一致，append 到数组即可）：
   {
     part:   'china' | 'world',        // 所属部分
     dim:    'trend' | 'strategy',      // 仅追加到「未来趋势」或「战略布局」维度
     id:     'auto-cn-strat-YYYYMMDD',  // 唯一 id（建议 auto-<部分>-<维度>-日期）
     title:  '标题',
     summary:'一句话摘要',
     body:   [ { p: '段落1' }, { p: '段落2' } ],
     points: [ '要点1', '要点2', '要点3' ],
     data:   '数据看点（可为空字符串 ""）',
     source: '来源：……（必填，含出处与日期）'
   }
   合法目标组合（part × dim）：
     china  × trend    | china  × strategy
     world  × trend    | world  × strategy
   ============================================================ */
const COUNTRY_AUTO_ENTRIES = [];
